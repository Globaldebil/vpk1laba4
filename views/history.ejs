<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>История конвертаций</h1>
            <p>Последние 50 операций</p>
        </div>

        <div class="nav">
            <a href="/" class="nav-link">Конвертер</a>
            <a href="/rates" class="nav-link">Курсы валют</a>
            <a href="/history" class="nav-link">История</a>
            <a href="/admin/rates" class="nav-link">Редактор</a>
            <form action="/logout" method="POST" style="display: inline;">
                <button type="submit" class="nav-link" style="background: none; border: none; cursor: pointer;">
                    Выйти (<%= user.username %>)
                </button>
            </form>
        </div>

        <% if (history.length === 0) { %>
            <div class="result">
                <p>История конвертаций пуста</p>
            </div>
        <% } else { %>
            <div class="converter-form">
                <table class="rates-table">
                    <thead>
                        <tr>
                            <th>Дата и время</th>
                            <th>Сумма</th>
                            <th>Из валюты</th>
                            <th>В валюту</th>
                            <th>Курс</th>
                            <th>Результат</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% history.forEach(record => { %>
                            <tr>
                                <td><%= new Date(record.convertedAt).toLocaleString('ru-RU') %></td>
                                <td><%= parseFloat(record.amount).toFixed(2) %></td>
                                <td><%= record.fromCurrency %></td>
                                <td><%= record.toCurrency %></td>
                                <td><%= parseFloat(record.rate).toFixed(6) %></td>
                                <td><strong><%= parseFloat(record.result).toFixed(2) %> <%= record.toCurrency %></strong></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>
</body>
</html>